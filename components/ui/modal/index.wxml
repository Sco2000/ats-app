<view
  wx:if="{{ visible }}"
  class="osn-modal-overlay {{ overlayClass }}"
  style="background-color: {{ overlayRGBA }}; backdrop-filter: blur({{ blur }}px);"
  bindtap="onOverlayTap"
>
  <view
    class="osn-modal-container {{ placement === 'bottom' ? 'osn-modal-container--bottom' : '' }} {{ containerClass }}"
    style="{{ computedStyle }} {{ containerStyle }}"
    catch:tap
  >
    <!-- Drag handle for bottom sheets -->
    <view wx:if="{{ placement === 'bottom' && showHandle }}" class="osn-modal-handle"></view>

    <view wx:if="{{ hasHeader }}" class="osn-modal-header" style="{{ headerStyle }}">
      <slot name="header"></slot>
    </view>

    <view wx:if="{{ hasContent }}" class="osn-modal-body" style="{{ bodyStyle }}">
      <slot name="content"></slot>
    </view>

    <view wx:if="{{ hasFooter }}" class="osn-modal-footer" style="{{ footerStyle }}">
      <slot name="footer"></slot>
    </view>
  </view>
</view>
