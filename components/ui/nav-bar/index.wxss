/* ============================================================================
   CUSTOM NAV BAR
   ============================================================================ */

.osn-nav-bar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 200;
  width: 100%;
  box-sizing: border-box;
  background: #ffffff;
}

.osn-nav-bar--transparent {
  background: transparent;
}

.osn-nav-bar__content {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 24rpx;
  position: relative;
}

.osn-nav-bar__logo {
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
}

.osn-nav-bar__back {
  position: absolute;
  left: 24rpx;
  top: 50%;
  width: 72rpx;
  height: 72rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.05);
  transition: background 0.15s ease-out;
  transform: translateY(-50%);
}

.osn-nav-bar__back:active {
  background: rgba(0, 0, 0, 0.1);
}

.osn-nav-bar__back-icon {
  font-size: 44rpx;
  font-weight: 700;
  color: #333;
  line-height: 1;
  margin-top: -4rpx;
}

.osn-nav-bar__title {
  font-size: 32rpx;
  font-weight: 700;
  color: #333;
  text-align: center;
}

.osn-nav-bar__title-wrap {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 96rpx;
}

.osn-nav-bar__back--hover {
  opacity: 0.6;
  background: rgba(0, 0, 0, 0.05) !important;
  transition: all 0.15s ease-out;
}
