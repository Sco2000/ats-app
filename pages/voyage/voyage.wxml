<app-layout
  title="Mes Voyages"
  showBack="{{true}}"
  logoSrc="/assets/images/logo.png"
  showLogo="{{false}}"
  transparent="{{false}}"
  activeTab="{{activeTab}}"
>
  <view class="content">
     <!-- sen contenue yii fii boulen doff -->
  </view>
</app-layout>
