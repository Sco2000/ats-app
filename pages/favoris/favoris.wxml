<view class="page">
    <view class="content">
          <view class="navbar">

            <app-nav-bar 
            title="Mes Favoris" 
            showBack="{{true}}"
            logoSrc="/assets/images/logo.png"
            showLogo="{{false}}"
            transparent ="{{false}}"
            />

          </view>

           <!-- sen contenue yii fii boulen doff -->
    </view>

  <app-tab-bar
    tabs="{{ tabs }}"
    activeTab="{{ activeTab }}"
  />
</view>
